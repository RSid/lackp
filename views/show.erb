<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Kickball Stuff</title>
    <link rel="stylesheet" href="kick.css" />
  </head>

  <body>
    <h1>Kickball teams</h1>

    <ul>

      <% @teams.each do |team| %>
        <li><a href= <%= "team_rosters/#{team}" %>><%= team.tr("_"," ") %></a></li>


      <% end %>
      </ul>

      <h1>Kickball Positions</h1>
      <ul>

      <% @positions.each do |position| %>
        <li><a href= <%= "positions/#{position}" %>><%= position.tr("_"," ") %></a></li>

      <% end %>
      </ul>

  </body>
</html>
